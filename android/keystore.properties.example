# 本地签名配置模板 —— 复制为 `android/keystore.properties` 并填入真实值。
#
# `android/keystore.properties` 与 keystore 文件本体都在根 `.gitignore` 里，
# **绝不可提交到版本库**（提交一次即等于泄露，且无法撤回）。
#
# storeFile 是相对 `android/` 目录的路径。
storeFile=thunder-release.keystore
storePassword=CHANGE_ME
keyAlias=thunder
keyPassword=CHANGE_ME
