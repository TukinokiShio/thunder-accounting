# 雷霆记账 v1.15.0 遗留问题收口台账

## 决策记录

| 编号 | 来源 | 决策 | 状态 |
|---|---|---|---|
| Q1/5 | 用户明确指令 | 已安装的 `v1.14.14` 作为回滚基线；`v1.14.15` 与 `v1.14.16` 作废；下一版直接为 `v1.15.0` | 已确认 |
| Q2/5 | 遗留报告 P0 | 旧 `VERIFY`/`USER_ACCEPT_FINAL` 状态不可继承，必须重启并保留真实验收门 | 已确认 |
| Q3/5 | 遗留报告 P1-1 | Aurora 合同必须可被脚本读取 | 已验证 |
| Q4/5 | 遗留报告 P1-2/P1-3 | 需求逐项落账；视觉证据与用户验收不能用编译结果替代 | 进行中 |
| Q5/5 | 遗留报告蓝色泄漏 | 主题语义统一使用项目 token，禁止通过全局覆盖掩盖硬编码 | 已验证/持续门禁 |

## 逐项需求

| ID | 来源 | 验收标准 | 当前状态 | 证据 |
|---|---|---|---|---|
| REQ-001 | P0-1/P0-2 | 旧完成状态不再作为本轮输入；状态回到可执行阶段，验收未被冒充为通过 | 进行中 | `progress.state` |
| REQ-002 | P1-1 | `DESIGN.md` 有 Product、Aurora compilation、Components and states 三段 | 已验证 | `DESIGN.md` |
| REQ-003 | P1-2 | 报告中的每项遗留要求具有来源、标准、状态和证据路径 | 已验证 | 本文件 |
| REQ-004 | P1-3 | 具备 light/dark、页面、640/1024/1440、focus/error/empty/loading/disabled/recovery 的真实证据 | 阻塞 | 用户运行后的截图/验收 |
| REQ-005 | 蓝色泄漏 | 源码不使用旧 blue/primary utility 或图表硬编码蓝色；主题测试和扫描通过 | 进行中 | `src/theme-contract.test.ts`、测试输出 |
| REQ-006 | 用户版本决策/交付规则 | 源码、安装脚本、安装包和 `app.asar` 全部为 `1.15.0` | 待构建 | `package*.json`、`scripts/thunder-setup.iss`、`exe` |
| REQ-007 | 交付安全 | 不自动覆盖用户已安装的 `v1.14.14`；不弹出作废版本安装包 | 已确认 | 默认安装路径检查、任务计划 |

## 证据边界

当前不声称用户已经完成 v1.15.0 的真实 GUI 验收。没有截图的状态只能是 pending，不能写成 pass。
