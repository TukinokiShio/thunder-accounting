# 雷霆记账 v1.16.0 接手、清理、UI 与 Windows 交付 PRD

## v1.16.1 增量变更（2026-08-29）

- 登录页焦点装饰收窄到实际输入框；底部文字按钮改为“忘记密码？”，保留“创建账号”入口；其它登录方式标题改为项目金棕色。
- 安装包输出目录改为 `release`，默认安装目录仍为 `exe`，Inno 安装向导允许用户自定义安装地址。
- 版本同步为 `1.16.1`，清理继续遵循 commit → push → 删除顺序。

## 决策记录

| 编号 | 来源 | 决策 | 状态 |
|---|---|---|---|
| Q1/5 | 用户指令 | 整理项目工作空间、把最终安装验收落到 `exe`、优化多个 UI 组件 | 已确认 |
| Q2/5 | 项目上下文 | 覆盖登录、应用外壳、首页、账单、统计、分类、个人中心、设置、表单、对话框和状态组件；保留业务行为、CloudBase、双语、同步与备份 | 已确认 |
| Q3/5 | SACW/Aurora 验收契约 | 测试、类型检查、Aurora lint、构建、Inno 打包、安装到 `exe`、三档视觉证据和用户 GUI 验收 | 已确认 |
| Q4/5 | 项目交付规则 | 不删除源码、Git 历史、用户数据或审计证据；只按用户确认的 cleanup manifest 清理可重建/作废产物；浅色默认、深色可切换 | 已确认 |
| Q5/5 | 失败路径定义 | 任一测试、lint、构建、打包、安装、版本校验或真实 GUI 验收失败，均不得标记 DONE，必须记录归因并回流 | 已确认 |
| V1 | 项目规则推导 | 当前 `1.15.2` 的兼容性 UI/交付增量采用 `1.16.0`；如用户指定其他版本，先更新本文件和计划 | 🔶 Assumption |
| C1 | 清理边界 | 具体旧产物逐项列入 cleanup manifest，用户确认后才删除/覆盖 | 🔵 Open Question |

## 逐项需求

| ID | 需求 | 可测试验收标准 | 状态 | 证据 |
|---|---|---|---|---|
| REQ-001 | 工作空间整理 | 产出清理清单；确认后仅删除清单项；删除后逐项验证不存在；源码/审计/回滚保护路径仍在 | 进行中 | `artifacts/cleanup-manifest-v1.16.0.json` |
| REQ-002 | 固定安装目录 | 安装包最终安装到 `E:\Code\CodeProduct\thunder-accounting\exe`；`exe/resources/app.asar` 存在且版本为 `1.16.0` | 待执行 | `artifacts/inno/inno-receipt.json` |
| REQ-003 | UI 统一 | 登录、外壳、业务页、表单、对话框、状态组件统一项目语义 token；无 blue/primary 色泄漏；保留交互行为 | 待执行 | `DESIGN.md`、源码扫描、测试 |
| REQ-004 | 主题与响应式 | 浅色默认、深色可切换；640/1024/1440 三档无横向溢出；焦点、错误、空、加载、禁用、恢复态有证据 | 待执行 | `artifacts/visual-evidence.json` |
| REQ-005 | 质量门 | Vitest、TypeScript、Aurora lint、构建、Inno 编译和安装断言全部通过 | 待执行 | `artifacts/run-receipt.json`、Inno receipt |
| REQ-006 | 用户验收 | 用户实机确认安装后的浅色/深色页面与关键交互，验收记录由用户回填，不能由执行者代填 | 待执行 | `user_signoff.md` |

## 边界场景

- 空账单、无搜索结果、加载中、错误可重试、离线/同步失败、禁用控件和权限提示。
- 表单空输入、格式错误、验证码过期、提交失败、焦点可见、键盘操作和 reduced-motion。
- 窄窗口、平板宽度、长中文/英文文案、系统缩放和深色主题。
- 无凭据的管理员账号不伪造登录成功；不把业务错误显示为模糊成功。

## 数据契约与失败路径

- 本轮不改 CloudBase/Auth/SQLite 数据结构和公开调用契约；UI 只消费现有 store、IPC 和翻译接口。
- 构建成功不等于交付成功；必须继续验证安装目录、`app.asar` 版本、应用启动和用户可见 UI。
- 任一硬门失败都写入 findings/progress，回流 EXEC；缺用户真实 GUI 验收时保持 pending。

## v1.17.0 增量变更：首页统计卡片明细弹窗（2026-09-12）

### 需求范围与非目标

首页 6 张统计卡片（今日支出 / 本月支出 / 日均支出 / 累计记录 / 本月收入 / 本月结余）当前为纯展示 `div`，
无法查看数字来源，UX 较差。本轮使其可点击并弹出对应明细弹窗。

**范围内**：新增弹窗组件（图形化组成拆解 + 逐笔明细）、首页接线、新增 i18n 词条、版本号升级、测试与打包验收。
**范围外（非目标）**：不新增 IPC 通道；不改数据库 / CloudBase / 认证 / Zustand 持久化；不做图表第二层下钻；不做导出；
不做跨月切换器；**不改卡片本体视觉**（用户明示已有点击动效）。

### 需求澄清记录（Qx/3，用户已答）

| 编号 | 问题 | 用户答复 |
|---|---|---|
| Q1/3 | 弹窗展示什么内容 | 「要能看到今天的每一笔支出，最好使用环形图等图像直观展示各笔支出，其他卡片同理」 |
| Q2/3 | 弹窗内条目支持哪些操作 | 「看情况，你认为怎样优化能够最大程度提高 UX，可加载 first-principle 辅助考虑」 |
| Q3/3 | 卡片点击视觉提示 | 「现在这六个卡片已有点击动效了，可以不改」 |

- 🔵 Q2 已由第一性原理推导回填：**明细只读、不内嵌编辑**（编辑入口已有两处，第三处属功能重复；弹窗职责单一 = 解释数字；符合"限制修改范围"红线）。
- 🔶 Assumption：日均支出 / 本月结余为**派生指标**（公式值），环形图语义不成立 → 日均用每日支出柱状趋势、结余用收入 vs 支出对比条 + 计算式。

### 逐项可验收标准

| ID | 验收标准 | 证据 |
|---|---|---|
| AC-1 | 6 张卡片均可点击并打开语义对应的弹窗；弹窗标题与卡片一致 | `StatCardDetailDialog.test.tsx`、`Home.test.tsx` |
| AC-2 | 弹窗列出的逐笔明细金额之和 = 顶部汇总金额（可加和不变量） | 单测断言 |
| AC-3 | 集合型卡片（今日/本月支出、本月收入、累计记录）提供按分类聚合的环形图；派生型（日均/结余）提供语义适配图形 | 单测 + 截图 |
| AC-4 | 卡片 DOM 结构、布局、hover 动效、深色主题与改动前一致 | `git diff` 不含卡片本体 |
| AC-5 | 版本号 `1.17.0` 在 `package.json`、`package-lock.json`、`scripts/thunder-setup.iss`、`exe\resources\app.asar` 四处一致 | 打包 + 安装校验 |
| AC-6 | 弹窗支持 Escape / X / 点击遮罩关闭；背景滚动锁定；`aria-modal` 语义 | 单测 |

### 数据安全不变量（硬约束）

- 弹窗**只读**：不写入数据库、不修改任何账单或分类记录。
- 写集严格限定为：`src/components/StatCardDetailDialog.tsx`、其测试、`src/pages/Home.tsx`、`Home.test.tsx`、
  `src/i18n/translations.ts`、`src/index.css`（仅追加）、版本元数据与流程产物。
- **禁止触碰**：`main-process/**`、`src/store/index.ts`、`src/pages/Stats.tsx`、`src/pages/Bills.tsx`、`resources/**`、
  任何 Electron `app.getPath('userData')` 下的 `.db` 文件。
- 弹窗数据通过现有只读 IPC（`getBills` / `getStats`）获取，使用显式日期范围，**不依赖 store 中可能被筛选过的 `bills`**。
- 执行后核对 `git diff --name-only`，出现数据层或无关页面变更立即回流，不进入打包。

### 边界场景

- 当日/当月零记录 → 弹窗显示「暂无记录」，不空白、不崩溃。
- 单笔记录 → 环形图为单一 100% 扇区，正常渲染。
- IPC 失败 → 显示错误提示，弹窗仍可关闭。
- 长分类名 / 长备注 → 明细行省略号截断，不产生横向溢出。
- 深色主题 / 640 / 1024 / 1440 宽度 → 布局与 token 一致。

### 失败路径定义

任一测试失败、版本号不一致、打包/安装失败、`git diff` 越界、卡片本体视觉回归、明细与汇总不可加和 → 回流 EXEC，
不标记完成。构建成功不等于交付成功，必须继续验证安装目录与 `app.asar` 版本。

---



### 范围与非目标

- 仅覆盖 `AddBillDialog` 与其使用的 `CategorySelect`：分类选择器金棕色主题，以及金额/日期/备注输入框的焦点边界。
- 不修改账单数据契约、校验/提交逻辑、Zustand、IPC、数据库、其他页面或分类管理页面。
- 不新增依赖；继续使用现有 `react-select` 5.10.2。

### 可验收标准

1. 分类一级/二级控件默认态使用项目 `--bg-card`、`--border`、`--text`/`--text3`；聚焦态使用 `--accent`，不出现 `react-select` 默认蓝色或默认双层阴影。
2. 分类菜单通过现有 Portal 显示在弹窗上方，菜单、悬停、选中、禁用和深色主题均消费项目语义 token。
3. 聚焦金额、日期或备注时，金棕色边界只覆盖实际输入框；标签不被 outline/光晕覆盖；每个控件只保留一层焦点反馈。
4. 分类联动、键盘操作、二级分类禁用态和弹窗原有提交/关闭行为保持不变。
5. 相关测试、TypeScript、生产构建和真实 GUI 截图/用户验收按项目交付规则执行。
6. 现有用户数据保持不变：本轮不读取、迁移、清空、覆盖或删除真实 `userData` 数据库；构建、打包和安装只处理应用产物，不触发数据写入。

### 数据安全不变量

- Worker 的源码写集仅限本节列出的记账弹窗渲染层、测试和版本元数据；禁止触碰 `main-process/database`、CloudBase、IPC、Zustand 数据持久化、`resources` 中用户数据以及任何 Electron `app.getPath('userData')` 文件。
- 行为测试使用临时目录/隔离 fixture，不连接或修改真实用户数据库；不以启动真实用户环境作为数据库验证手段。
- 执行前后核对 `git diff --name-only` 与契约写集；若出现数据层、迁移脚本、用户数据目录或无关页面变更，立即回流，不进入打包和安装。
- 安装验收只校验 `exe` 内的应用文件与 `app.asar` 版本；历史安装包、用户数据和回滚证据均不纳入清理范围。

### 失败路径与边界

- 任何测试失败、样式仍出现蓝色/越界、Portal 菜单被遮挡、主题不一致或业务行为回归，均回流 EXEC，不标记完成。
- 验收覆盖浅色/深色、640/1024/1440 宽度、默认/聚焦/悬停/选中/禁用、键盘 Escape/Tab 和 reduced-motion。
