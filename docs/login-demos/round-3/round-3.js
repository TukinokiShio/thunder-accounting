document.querySelectorAll('[data-r3-form]').forEach((form) => form.addEventListener('submit', (event) => { event.preventDefault(); const button = form.querySelector('.submit'); const text = button.textContent; button.textContent = 'Demo 预览中…'; window.setTimeout(() => { button.textContent = text }, 1200) }))
document.querySelectorAll('.auth-tabs').forEach((tabs) => tabs.querySelectorAll('button').forEach((button) => button.addEventListener('click', () => { tabs.querySelectorAll('button').forEach((item) => item.classList.toggle('active', item === button)); const title = document.querySelector('#auth-title'); if (title && button.textContent.trim() === '注册') title.textContent = '创建你的账本' })))
