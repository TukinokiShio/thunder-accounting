# 雷霆记账 CloudBase 环境变量
# 复制此文件为 .env 并填入真实值

# CloudBase Server API Key（用于云同步、验证码后的密码修改等服务端功能）
# 获取路径：CloudBase 控制台 → 身份认证 → API Key
# SDK 官方环境变量名为 CLOUDBASE_APIKEY；客户端同时兼容下方项目历史命名。
# 打包后的 EXE 不会内置真实密钥，请将 .env 放在 resources 或用户数据目录，或设置系统环境变量。
CLOUDBASE_API_KEY=your-api-key-here
